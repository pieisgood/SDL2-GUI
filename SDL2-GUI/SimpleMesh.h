#ifndef SIMPLE_MESH
#define SIMPLE_MESH
#include <GL\glew.h>
//VERY Unfinished, and unlikely to be included in SDL2_GUI
/*
class SimpleMesh {
private:
	float* m_vertex_array; 
	float* m_normal_array;
	float* m_tex_coord_array;
	short* m_index_array; //an array that indexes each triangle in our geometry
	GLuint m_vao; //our vertex array object for binding to the render buffer
	float m_numVertices; //the number of vertices in our mesh

public:




};
*/

#endif